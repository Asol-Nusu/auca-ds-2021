#include <bits/stdc++.h> 

template <typename C>
int sz(const C &c) { 
    return static_cast<int>(c.size()); 
}

using namespace std; //to avoid repetition

int main()
{
    iostream::sync_with_stdio(false); 
    int r = -91;
    int k = -30;
    cout << r % 2 << "\n";
} 