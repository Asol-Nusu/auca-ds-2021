Requirements for BigInt class:
- Signed Integer numbers of arbitrary length 
- The inner representation is a std::vector<int> (or std::vector<int64_t>) of digits
- Test w/ unit tests

- Make string constructor more advanced
- Work on + operator